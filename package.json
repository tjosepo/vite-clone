{
  "name": "windpack-monorepo",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "cd ./packages/template && pnpm run dev",
    "build": "cd ./packages/template && pnpm run build",
    "watch": "pnpm run --parallel watch"
  }
}